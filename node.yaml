apiVersion: v1
kind: Pod
metadata:
  name: ungdungnode
  labels:
    app: app1
    ungdung: ungdung1
spec:
  containers:
  - name: c1
    image: <Image>
    resources:`
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: <Port>
